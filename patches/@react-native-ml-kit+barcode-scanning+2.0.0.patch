diff --git a/node_modules/@react-native-ml-kit/barcode-scanning/android/src/main/java/com/rnmlkit/barcodescanning/BarcodeScanningModule.java b/node_modules/@react-native-ml-kit/barcode-scanning/android/src/main/java/com/rnmlkit/barcodescanning/BarcodeScanningModule.java
index 30323b4..2e52f5a 100644
--- a/node_modules/@react-native-ml-kit/barcode-scanning/android/src/main/java/com/rnmlkit/barcodescanning/BarcodeScanningModule.java
+++ b/node_modules/@react-native-ml-kit/barcode-scanning/android/src/main/java/com/rnmlkit/barcodescanning/BarcodeScanningModule.java
@@ -23,6 +23,7 @@ import com.google.mlkit.vision.barcode.common.Barcode;
 import com.google.mlkit.vision.barcode.BarcodeScanner;
 import com.google.mlkit.vision.barcode.BarcodeScannerOptions;
 import com.google.mlkit.vision.barcode.BarcodeScanning;
+import android.graphics.Point;
 import java.io.IOException;
 import java.util.List;
 import java.net.URL;
@@ -73,6 +74,31 @@ public class BarcodeScanningModule extends ReactContextBaseJavaModule {
                                 WritableMap map = Arguments.createMap();
                                 map.putString("value", barcode.getRawValue());
                                 map.putDouble("format", barcode.getFormat());
+                                
+                                // Extract corner points for accurate size calculation
+                                Point[] cornerPoints = barcode.getCornerPoints();
+                                if (cornerPoints != null && cornerPoints.length > 0) {
+                                    WritableArray corners = Arguments.createArray();
+                                    for (Point point : cornerPoints) {
+                                        WritableMap corner = Arguments.createMap();
+                                        corner.putDouble("x", point.x);
+                                        corner.putDouble("y", point.y);
+                                        corners.pushMap(corner);
+                                    }
+                                    map.putArray("cornerPoints", corners);
+                                }
+                                
+                                // Also provide bounding box for convenience
+                                android.graphics.Rect boundingBox = barcode.getBoundingBox();
+                                if (boundingBox != null) {
+                                    WritableMap bounds = Arguments.createMap();
+                                    bounds.putDouble("left", boundingBox.left);
+                                    bounds.putDouble("top", boundingBox.top);
+                                    bounds.putDouble("width", boundingBox.width());
+                                    bounds.putDouble("height", boundingBox.height());
+                                    map.putMap("boundingBox", bounds);
+                                }
+                                
                                 result.pushMap(map);
                             }
                             promise.resolve(result);
diff --git a/node_modules/@react-native-ml-kit/barcode-scanning/ios/BarcodeScanning.m b/node_modules/@react-native-ml-kit/barcode-scanning/ios/BarcodeScanning.m
index 6ce5445..3934866 100644
--- a/node_modules/@react-native-ml-kit/barcode-scanning/ios/BarcodeScanning.m
+++ b/node_modules/@react-native-ml-kit/barcode-scanning/ios/BarcodeScanning.m
@@ -41,10 +41,36 @@ @implementation BarcodeScanning
                     format = @(barcode.format);
             }
               
-            [result addObject:@{
-              @"value": barcode.displayValue,
+            NSMutableDictionary *barcodeData = [NSMutableDictionary dictionaryWithDictionary:@{
+              @"value": barcode.displayValue ?: @"",
               @"format": format
             }];
+            
+            // Extract corner points for accurate size calculation
+            NSArray<MLKPoint *> *cornerPoints = barcode.cornerPoints;
+            if (cornerPoints && cornerPoints.count > 0) {
+              NSMutableArray *corners = [NSMutableArray array];
+              for (MLKPoint *point in cornerPoints) {
+                [corners addObject:@{
+                  @"x": @(point.x),
+                  @"y": @(point.y)
+                }];
+              }
+              barcodeData[@"cornerPoints"] = corners;
+            }
+            
+            // Also provide frame (bounding box) for convenience
+            CGRect frame = barcode.frame;
+            if (!CGRectIsEmpty(frame)) {
+              barcodeData[@"boundingBox"] = @{
+                @"left": @(frame.origin.x),
+                @"top": @(frame.origin.y),
+                @"width": @(frame.size.width),
+                @"height": @(frame.size.height)
+              };
+            }
+              
+            [result addObject:barcodeData];
           }
 
           resolve(result);
